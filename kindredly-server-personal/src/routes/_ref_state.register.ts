// Helper import so route discovery systems that glob-import routes can pick up RefStateRoute.
// If your server registers routes manually, you can ignore this file and add RefStateRoute
// to the central route list instead.

import RefStateRoute from './ref_state.route';

export default RefStateRoute;
